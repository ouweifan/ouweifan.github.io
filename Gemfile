source "https://rubygems.org"

# github-pages 会把本地 Jekyll 版本锁定到与 GitHub Pages 线上环境一致，
# 避免“本地能跑、线上构建报错”的情况。
gem "github-pages", group: :jekyll_plugins

# Ruby 3.0+ 不再自带 webrick，本地 `jekyll serve` 需要它。
gem "webrick", "~> 1.8"
